import { handleSignOut } from '../Registration/registerManager';
import Sidebar from '../Sidebar/Sidebar';

const Home = () => {
  return (
    <div>
      <Sidebar></Sidebar>
    </div>
  );
};

export default Home;
